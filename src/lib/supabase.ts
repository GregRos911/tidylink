
import { supabase } from "@/integrations/supabase/client";

// Create Supabase helper functions for common operations
export const supabaseHelpers = {
  // Add any common helper functions here
};

export default supabaseHelpers;
